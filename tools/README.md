# Tools

Shared helper scripts installed to `~/.claude/skills/_shared/` by `install.sh`. They support optional external reviews, reflection path generation, and docs catalog scaffolding for the Claude workflow skills.
